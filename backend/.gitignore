node_modules/
baileys-auth/
.env
.env.local
.env.development
.env.production
# Keep .env.example tracked (placeholders only)
!.env.example
# Ignore all uploaded images (user content lives on a volume/object storage in
# production, NOT in the repo) — but keep the folder itself via .gitkeep.
uploads/*
!uploads/.gitkeep
.data/
